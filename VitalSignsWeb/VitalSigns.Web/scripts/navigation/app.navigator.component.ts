import {Component} from '@angular/core';
import {ROUTER_DIRECTIVES} from '@angular/router';

@Component({
    selector: 'app-navigator',
    templateUrl: '/partial/navigator',
    directives: [ROUTER_DIRECTIVES]
})
export class AppNavigator { }